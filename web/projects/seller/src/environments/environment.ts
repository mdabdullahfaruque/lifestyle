export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:5000',
  // Empty in development: Google rejects an origin it has not been told about, so the button is
  // hidden locally rather than rendered and broken.
  googleClientId: '',
};
