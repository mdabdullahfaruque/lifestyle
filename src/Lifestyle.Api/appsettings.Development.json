{
  "ConnectionStrings": {
    "Default": "Host=localhost;Port=5432;Database=lifestyle;Username=lifestyle;Password=lifestyle"
  },

  "Platform": {
    "CorsOrigins": [
      "http://localhost:4200",
      "http://localhost:4201",
      "http://localhost:4202"
    ]
  },

  "Identity": {
    "JwtIssuer": "https://localhost:5001",
    "//JwtSigningKey": "Development only. Production supplies this from the secret store, never from a file.",
    "JwtSigningKey": "dev-only-insecure-signing-key-change-me-32b+",
    "PasswordResetUrls": {
      "buyer": "http://localhost:4200/reset-password",
      "seller": "http://localhost:4201/reset-password",
      "admin": "http://localhost:4202/reset-password"
    }
  },

  "//Email": "Left blank on purpose: the logging sender writes the reset link into the API log, so a reset can be completed locally with no mail server. Point Host at 127.0.0.1:1025 with UseStartTls false to use the Mailpit container instead.",
  "Email": {
    "Host": ""
  },

  "Vendors": {
    "SellerConsoleUrl": "http://localhost:4201",
    "ShopUrlTemplate": "http://localhost:4200/shop/{slug}"
  },

  "Seed": {
    "SuperAdmin": {
      "//": "Set these before running `dotnet run -- seed`. Never commit real credentials.",
      "Email": "admin@lifestyle.test",
      "Password": "ChangeMeLocally!42",
      "FullName": "Local Administrator"
    }
  },

  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": {
        "Microsoft.AspNetCore": "Information",
        "Microsoft.EntityFrameworkCore.Database.Command": "Information"
      }
    }
  }
}
